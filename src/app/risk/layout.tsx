export default function RiskLayout({ children }: { children: React.ReactNode }) {
  return (
    <div id="risk-monitor-page" className="risk-monitor-theme -mx-4 sm:-mx-6 lg:-mx-8">
      {children}
    </div>
  );
}
